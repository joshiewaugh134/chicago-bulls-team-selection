ATTOVR <- c(pg_data$ATTOVR) 
mean_ATTOVR <- mean(ATTOVR)            #finding mean ATTOVR
mean_AST <- mean(pg_data$AST)          #finding mean AST
mean_TOV <- mean(pg_data$TOV)          #finding mean TOV